#ifndef ATOM_IMAGE_LOADER_H
#define ATOM_IMAGE_LOADER_H

#include "../Defines.h"
#include "webgpu/webgpu_cpp.h"

namespace atom
{

//class ImageLoader
//{
//public:
//	static wgpu::Texture LoadTexture(const char* path, wgpu::Device& device, wgpu::TextureView* pTextureView);
//private:
//	static void WriteMipMaps(const wgpu::Device& device, wgpu::Texture texture,
//				wgpu::Extent3D textureSize, [[maybe_unused]] u32 mipLevelCount, // not used yet
//				const unsigned char* pixelData);
//};

} // namespace atom

#endif // ATOM_IMAGE_LOADER_H